class ModuleDocFragment(object):
    DOCUMENTATION = r"""
        portal:
            description:
                - EdwinAI portal to target.
                - For example, a domain of `foo.dexda.ai` has a portal of `foo`.
            required: true
            type: str
    """
